import { create } from '@storybook/theming'

import logo from './logo.svg'

export default create({
    base: 'light',
    appBorderColor: 'white',
    brandImage: logo,
    brandTitle: 'Provincie Zuid-Holland',
})
